version: '2'
services: 
  srs-postgresql:
    extends:
      file: sgt-postgresql.yaml
      service: sgt-postgresql
    container_name: sgt-postgresql

  rabbitmq:
    extends:
      file: rabbitmq.yaml
      service: rabbitmq
    container_name: rabbitmq

  elasticsearch:
    extends:
      file: elasticsearch.yaml
      service: elasticsearch
    container_name: elasticsearch
  